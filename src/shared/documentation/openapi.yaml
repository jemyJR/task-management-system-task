openapi: 3.0.0
info:
  title: Task Management API Documentation
  version: 1.0.0
  description: |
    API documentation for Task Management platform.
  contact:
    name: Task Management API Support

servers:
  - url: /api/v1
    description: API server

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
